{
  "name": "@vortr/mcp",
  "version": "0.1.8",
  "description": "Vortr MCP — non-custodial DeFi swap tools on Base for AI agents (search tokens, 0x quotes, ERC-5792 approve+swap calldata). Your agent signs + sends the ERC-5792 payload with its own wallet, or runs the @vortr/wallet local signer for autonomous execution; Vortr never signs.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "defi",
    "base",
    "0x",
    "swap",
    "dex",
    "ai-agent",
    "vortr",
    "non-custodial"
  ],
  "homepage": "https://vortragents.com",
  "repository": { "type": "git", "url": "git+https://github.com/vortr/sdk.git", "directory": "packages/mcp" },
  "license": "MIT",
  "type": "module",
  "bin": { "vortr-mcp": "dist/index.js" },
  "files": ["dist", "llms.txt", "SKILL.md", "README.md"],
  "publishConfig": { "access": "public" },
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "viem": "^2.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@vortr/core": "workspace:*",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0",
    "@types/node": "^25.9.1"
  }
}
