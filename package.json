{
  "name": "vortr-sdk",
  "private": true,
  "description": "Open-source Vortr packages — non-custodial DeFi swaps on Base for AI agents.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "sync-mirrors": "node scripts/sync-mirrors.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
